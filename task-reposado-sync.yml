# vim: et:ts=4:sw=4
version: '2'
services:
    reposado-sync:
        extends:
            file: docker-compose.yml
            service: reposado
        command: /reposado/code/repo_sync --log /dev/stdout
